import tkinter as tk

class PlaatjesWindow(tk.Frame):
    def __init__(self, parent, imageNames, *args, **kwargs):
        tk.Frame.__init__(self, *args, **kwargs)
        self.imageList = imageNames
        self.parent = parent

        #<create the rest of your GUI here>

    def TestLogic(self):
        global index
        index = index + 1
        im = Image.open(imageList[index])
        stgImg = ImageTk.PhotoImage(im)
        label.configure(image=stgImg)
        label.image = stgImg

    def Verwijder(self):
        global changeList
        changeList.append("verwijder", )



# root = tk.Tk()
#
# root.geometry('1010x740+0+200')
#
# stgImg = ImageTk.PhotoImage(Image.open(imageList[index]))
# label = ttk.Label(root, image=stgImg)
# label.place(x=0, y=0)
#
# testBtn = ttk.Button(root, text="TEST", command=TestLogic)
# testBtn.place(x=400, y=200)
# root.mainloop()